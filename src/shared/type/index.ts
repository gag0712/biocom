export type User = {
  email: string;
  password: string;
  name: string;
  mobile: string;
};

export type Product = {
  id: string;
  name: string;
  price: number;
  description: string;
  image: string;
  category: string;
  stock: number;
};
